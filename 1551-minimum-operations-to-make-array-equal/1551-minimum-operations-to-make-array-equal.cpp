class Solution {
public:
    int minOperations(int n) {
        // vector<int>nums(n,0);
        // for(int i = 0;i<n;i++)
        // {
        //     nums[i] = (2 * i) + 1;
        // }
        // int x = 0;
        // int sum = 0;
        // for(int i : nums)
        // {
        //     sum = sum + i;
        // }
        // int avg = sum/n;
        // int y= nums.size()-1;
        // while(x<y)
        // {

        // }
        return (n*n)/4;
    }
};